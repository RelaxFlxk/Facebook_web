export const actions = {
  // INIT_LEVEL ({ commit }, data) {
  //   const result = this.callBeTaskAPI('get', '/level/line/get?RECORD_STATUS=N')
  //   if (result) {
  //     if (result.length > 0) {
  //       console.log(result)
  //       commit('SET_LEVEL', result)
  //     }
  //   }
  // }
}
